# 跨題目盲點彙總

> 每次寫完 `刷題詳細記錄/` 裡的一段紀錄後，若盲點是「重複出現」而非單題特例，補一行到下面對應維度。用來判斷三個月後整體弱在哪一類，而不用逐題翻。

## 1️⃣ 語法概念

| 日期 | 題號 | 盲點 | 是否已解決 |
|------|------|------|-----------|
| 2026-08-11 | 2, 4, 6, 7, 9, 12 | 關鍵字/函式名打字錯誤，多半是其他語言（C#/JS）的命名習慣帶過來（`esle`、`var`、`reutrn`、`.Size()`／`.length()` 用錯容器的方法、`unorder_map`／`unorder_set` 少打字、`null` 應為 `nullptr`），編譯期就會被抓到，但反覆出現 | 追蹤中 |
| 2026-08-17 | 3, 9 | **指標（pointer）跟參考（reference）混淆**：對參考多此一舉取址／解參考（`&list3` 誤用、`*image`／`->first` 把參考當指標或把 vector 當 pair），第 2 次出現，需要刻意分清楚「手上拿到的是物件本身/參考，還是位址」 | 追蹤中，反覆出現 |
| 2026-08-21 | 8, 14 | **二分法算中間索引忘記加回 `head` 偏移量**：`mid = (tail-head)/2` 而不是 `head + (tail-head)/2`，第 2 次犯一模一樣的錯誤（Binary Search、First Bad Version），這個公式要練到直接肌肉記憶 | 追蹤中，反覆出現 |

## 2️⃣ 邏輯與複雜度

| 日期 | 題號 | 盲點 | 是否已解決 |
|------|------|------|-----------|
| 2026-08-10 | 2, 3, 8, 9, 12 | 迴圈/遞迴裡的狀態變數管理反覆出錯：該更新卻沒更新（tail 指標、二分法中間索引），該固定卻被重新計算（Flood Fill 的 `curColor`），或**該跨層共用卻宣告成區域變數、每層被重新初始化**（Linked List Cycle 的 `unordered_set` 配合遞迴，每次呼叫都變回空的）。第 5 次出現，是目前最主要的弱點，需要刻意注意：**遞迴/迴圈裡，先想清楚哪些值該隨迭代更新、哪些值該保持不變用參數傳遞、哪些值該跨層共用不能宣告在會被重新初始化的位置** | 追蹤中，反覆出現，主要弱點 |

## 3️⃣ 演算法 / 資料結構盲點

| 日期 | 題號 | 盲點 | 是否已解決 |
|------|------|------|-----------|
| 2026-08-07 | 1, 2 | 遇到題目沒有第一時間辨識出對應的標準資料結構（互補查找→Hash Map、巢狀配對→Stack），第一直覺先寫暴力/統計法，靠提示才想到正解方向 | 追蹤中 |
| 2026-08-11 | 4, 6 | 沒有先確認清楚題目輸入資料的實際定義就開始設計方向（誤用排序類結構、把「已經是完整樹的輸入」誤解成「待插入的原始數值」），導致整個方向錯誤、大量時間花在推翻重來 | 追蹤中 |
| 2026-08-24 | 15, 16 | **`pair` 跟「多筆資料容器」搞混**：需要同時追蹤很多筆 key 對應資料時（字元次數、DP 子問題答案），直覺先想到 `pair`（只能裝一組），第 2 次出現（Ransom Note、Climbing Stairs），已整理進 `container_selection.md` 速查表，之後遇到「要存很多筆」先反問自己這句話 | 追蹤中，反覆出現 |
