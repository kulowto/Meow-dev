---
type: template
scope: Photoshop
version: v1.0
created: 2026-05-13
usage: 每份 Photoshop 工具文件都必須複製此骨架填寫，並存放至 Meow-Toolbox/Photoshop/tools/
---

# (Template) PS 工具文件骨架

> 此骨架是所有 Photoshop 工具文件的規範，AI 生成或更新任何 PS 工具文件時，
> 必須遵守此結構，不可省略任何欄位（非必填欄位填「無」即可）。

---

## 功能名稱

`[tool_id]`：工具中文名稱

> `tool_id` 使用英文小寫加底線，例如：`outline_border`、`halftone_shadow`

---

## 描述

> AI 判斷「什麼情況下應該調用這個工具」的依據。
> 說明這個工具的核心功能，以及適合被調用的情境。

[填入說明]

---

## 前置條件

> 調用此工具前，Photoshop 文件與環境必須滿足以下條件。
> AI 組合多個工具時，需先確認前置條件全部通過，才可執行。

- [ ] 條件 1（例：`src_main` 圖層已存在且完成去背）
- [ ] 條件 2（例：`chr_effects` 群組已建立）

---

## 輸入 / 輸出定義

### 輸入

| 參數名稱 | 類型 | 必填 | 說明 |
|---------|------|------|------|
| `app` | PS Application | ✅ | Photoshop 應用程式物件 |
| `doc` | PS Document | ✅ | 目標文件物件 |
| `chr_layer` | PS Layer | ✅ | 角色主圖層（src_main）reference |
| `effects_group` | PS LayerSet | ✅ | chr_effects 群組 reference |
| *(其他參數)* | | | |

### 輸出

> 執行完畢後，文件的狀態改變為：

- 新增圖層：`[圖層名稱]` 位於 `[群組名稱]` 的 `[位置（最上 / 最下 / 指定圖層前後）]`
- 圖層屬性：混合模式 `[Normal / Multiply / ...]`、鎖定狀態 `[是 / 否]`

---

## 步驟

> 完整執行流程，依序列出。此清單即為 AI 執行時的操作依據。

1. 步驟一
2. 步驟二
3. ...

---

## 工具（函式清單）

> 記錄此工具調用了哪些函式，用於維護追蹤與未來重構。

| 函式名稱 | 所在檔案 | 用途 |
|---------|---------|------|
| `function_name()` | `ps_auto.py` | 說明 |

---

## 圖層區域與優先級

> 決定輸出圖層在 PSD 中的最終位置。
> Zone 定義與排序規則詳見：`Meow-Toolbox/Photoshop/(AI_Read) PSD Master Blueprint.md`
>
> ⚠️ 只有在最終排列圖層順序時，才需要查閱 Blueprint；單獨執行本工具不需要。

| 欄位 | 值 |
|------|---|
| `layer_zone` | `[EFFECT_OVERLAY / CHARACTER / EFFECT_UNDERLAY / BACKGROUND]` |
| `layer_priority` | `[數字，越小越靠上層]` |

---

## 參數

> 可在執行前調整的數值，對應程式碼中的可調常數。

| 參數名稱 | 預設值 | 說明 |
|---------|--------|------|
| `PARAM_NAME` | 預設值 | 說明這個參數影響什麼效果 |

---

## 副檔（條件性參考）

> 非必要資訊，只在特定情境下需要查閱。保留擴充空間。
> 若無，填「無」。

| 文件 | 觸發條件 | 說明 |
|------|---------|------|
| `(AI_Read) xxx.md` | 當 ... 時 | 說明 |

---

## 驗證標準

> 執行完畢後，用來確認結果正確的檢查清單。
> 對應「單元測試」的通過條件。

- [ ] 驗證條件 1（例：`chr_outerLine` 存在於 `chr_effects` 群組內）
- [ ] 驗證條件 2（例：混合模式為 Multiply）
- [ ] 驗證條件 3（例：圖層位於 chr_effects 的最下層）

---

## 常見錯誤記錄

> 可持續更新。記錄執行過遇到的錯誤，讓未來的 AI 不重蹈覆轍。

| 錯誤描述 | 原因 | 解法 |
|---------|------|------|
| （初始為空，遇到問題再補充） | — | — |

---

## 告知訊息（使用者說明）

> 執行前需要向使用者說明的資訊，以及引導使用者選擇參數。

### 使用前說明

[向使用者說明這個工具的視覺效果與注意事項]

### 參數選擇提示

> 對每個可調參數，提供三個預設方案 + 自訂輸入。

**`[參數名稱]`**：[說明這個參數影響什麼視覺效果]

| 選項 | 數值 | 效果說明 |
|------|------|---------|
| 小 | `[數值]` | [效果描述] |
| 中（預設）| `[數值]` | [效果描述] |
| 大 | `[數值]` | [效果描述] |
| 自訂 | 使用者輸入 | 直接輸入數值，不限範圍 |
